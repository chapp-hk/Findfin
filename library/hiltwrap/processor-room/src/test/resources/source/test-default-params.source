package ch.app.library.hiltwrap.room

import androidx.room.Dao
import androidx.room.Database
import androidx.room.Entity
import androidx.room.Insert
import androidx.room.RoomDatabase
import ch.app.library.hiltwrap.annotation.HiltWrapRoomDao
import ch.app.library.hiltwrap.annotation.HiltWrapRoomModule

@Entity
data class TestEntity1(val id: Int)
@Dao
interface TestDao1 {
    @Insert fun insert(item: TestEntity1)
}

@Entity
data class TestEntity2(val id: Int)
@Dao
interface TestDao2 {
    @Insert fun insert(item: TestEntity2)
}

@Database(
    entities = [
        TestEntity1::class,
        TestEntity2::class,
    ],
    version = 1,
)
@HiltWrapRoomModule(databaseName = "test_database")
internal abstract class TestDatabaseDefaultParameters : RoomDatabase() {
    @HiltWrapRoomDao abstract fun testDao1(): TestDao1
    @HiltWrapRoomDao abstract val testDao2: TestDao2
}